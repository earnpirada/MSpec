classdef PreprocessedMSAData < handle
    
    properties
        PreprocessedSpectra % include until norm
        MinPosition %min pos to divide segment
        SegmentLag %segment lag
        ShiftedSegment %shifted segment
        BinIndexList
        BinnedSpectra
    end
    
    methods
    end
end

